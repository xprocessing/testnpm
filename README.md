# API 服务器 Demo

一个基于 Node.js + Express 的 RESTful API 服务器，内置美观的 API 测试界面。

## 功能特性

- 🚀 完整的 RESTful API 实现
- 📦 用户和文章管理模块
- 🧪 内置 API 测试界面
- 🎨 现代化 UI 设计
- 📝 详细的请求/响应显示
- ⚡ 快速选择预设 API 端点

## 快速开始

### 安装依赖

```bash
npm install
```

### 启动服务器

```bash
npm start
```

服务器将在 `http://localhost:3000` 启动

### 开发模式（自动重启）

```bash
npm run dev
```

## 使用说明

启动服务器后，访问以下地址：

- **首页/API测试页**: http://localhost:3000

在测试页面中，你可以：

1. 从左侧快速选择预设的 API 端点
2. 自定义请求方法、端点和请求体
3. 点击"发送请求"测试 API
4. 查看详细的响应结果（包括状态码、响应时间、响应数据）
5. 复制响应结果

## API 端点

### 用户管理

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/users` | 获取所有用户 |
| GET | `/api/users/:id` | 根据ID获取用户 |
| POST | `/api/users` | 创建用户 |
| PUT | `/api/users/:id` | 更新用户 |
| DELETE | `/api/users/:id` | 删除用户 |

**用户数据结构**:
```json
{
  "id": 1,
  "name": "张三",
  "email": "zhangsan@example.com",
  "age": 25
}
```

### 文章管理

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/posts` | 获取所有文章 |
| GET | `/api/posts/:id` | 根据ID获取文章 |
| POST | `/api/posts` | 创建文章 |

**文章数据结构**:
```json
{
  "id": 1,
  "title": "文章标题",
  "content": "文章内容",
  "authorId": 1,
  "createdAt": "2024-01-01"
}
```

### 测试接口

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/test/echo` | 回显测试 - 返回接收到的数据 |
| GET | `/api/test/delay?ms=1000` | 延迟测试 - 延迟指定毫秒后响应 |
| POST | `/api/test/validate` | 数据验证 - 验证用户名和密码格式 |

## 项目结构

```
testnpm/
├── server.js              # 主服务器文件
├── package.json           # 项目配置文件
├── .gitignore            # Git 忽略文件
└── public/
    └── index.html        # API 测试界面
```

## 技术栈

- **后端**: Node.js + Express
- **中间件**: cors（跨域支持）
- **前端**: 原生 HTML/CSS/JavaScript
- **开发工具**: nodemon（开发模式自动重启）

## 请求示例

### 创建用户

```bash
curl -X POST http://localhost:3000/api/users \
  -H "Content-Type: application/json" \
  -d '{"name":"张三","email":"zhangsan@example.com","age":25}'
```

### 获取用户列表

```bash
curl http://localhost:3000/api/users
```

### 更新用户

```bash
curl -X PUT http://localhost:3000/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name":"李四","email":"lisi@example.com","age":30}'
```

### 删除用户

```bash
curl -X DELETE http://localhost:3000/api/users/1
```

### 测试回显

```bash
curl -X POST http://localhost:3000/api/test/echo \
  -H "Content-Type: application/json" \
  -d '{"message":"测试数据"}'
```

## 响应格式

所有 API 响应遵循统一格式：

```json
{
  "success": true/false,
  "data": {...},
  "message": "操作结果说明"
}
```

## 开发说明

如需修改端口号，可在启动时设置环境变量：

```bash
# Windows
set PORT=3001 && npm start

# Linux/Mac
PORT=3001 npm start
```

## 许可证

ISC
